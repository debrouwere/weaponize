should = require 'should'
weaponize = require '../src'
fs = require 'fs'
fs.path = require 'path'

# example = __dirname + '/examples/advanced/index.html'
# destination = __dirname + '/tmp'

it 'can load a file', (done) ->

it 'can write a file', (done) ->

it 'can preprocess or compile a file', (done) ->

it 'can set a file to a certain environment with `envv`', (done) ->