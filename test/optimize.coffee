should = require 'should'
weaponize = require '../src'
fs = require 'fs'
fs.path = require 'path'

# example = __dirname + '/examples/advanced/index.html'
# destination = __dirname + '/tmp'

it 'can rename a file based on the transformations it goes through', (done) ->

it 'can concatenate JavaScript into a single file', (done) ->

it 'can concatenate CSS into a single file', (done) ->

it 'can minify HTML', (done) ->

it 'can minify JavaScript', (done) ->

it 'can minify CSS', (done) ->