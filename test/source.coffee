should = require 'should'
weaponize = require '../src'
fs = require 'fs'
fs.path = require 'path'

# example = __dirname + '/examples/advanced/index.html'
# destination = __dirname + '/tmp'

it 'can recursively find all files in a directory', (done) ->

it 'provides a buffer interface that is identical
    to the file-based one', (done) ->