should = require 'should'
weaponize = require '../src'
fs = require 'fs'
fs.path = require 'path'

# example = __dirname + '/examples/advanced/index.html'
# destination = __dirname + '/tmp'

it 'can find JavaScript and CSS links in a file'
(done) ->

it 'can rename a file based on the transformations it goes through'
(done) ->
    # .correct

it 'can check an optimized bundle of CSS or JavaScript against a 
    page and see what is missing'
(done) ->

it 'can return optimized CSS and JavaScript link references, 
    ready to be injected into a page its HTML'
(done) ->

it 'can concatenate JavaScript into a single file'
(done) ->